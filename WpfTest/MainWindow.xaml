<Window x:Class="WpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTest"
        xmlns:converter="clr-namespace:WpfTest.Converter"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="300">
    <Window.Resources>
        <converter:GenderConverter x:Key="converter"/>
    </Window.Resources>
    <Grid>
        <Grid.Resources>
            <Style TargetType="RadioButton">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Content" Value="hello"/>
            </Style>
        </Grid.Resources>
        <StackPanel Orientation="Horizontal" Height="50" VerticalAlignment="Top">
            <RadioButton Content="男"  Margin="10" 
                         IsChecked="{Binding  Model.Gender, Converter={StaticResource converter}, ConverterParameter=1}"/>
            <RadioButton Content="女" 
                         IsChecked="{Binding  Model.Gender, Converter={StaticResource converter}, ConverterParameter=2}"/>
        </StackPanel>
    </Grid>
</Window>
