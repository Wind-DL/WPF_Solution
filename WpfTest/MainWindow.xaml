<Window x:Class="WpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTest"
        xmlns:converter="clr-namespace:WpfTest.Converter"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="300">
    <Window.Resources>
        <converter:GenderConverter x:Key="converter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="Page1" VerticalAlignment="Center" Margin="2" 
                         Command="{Binding Model.CommandBase}" CommandParameter="Page1"/>
            <RadioButton Content="Page2" VerticalAlignment="Center" Margin="2"
                         Command="{Binding Model.CommandBase}" CommandParameter="Page2"/>
        </StackPanel>
        <ContentControl Grid.Row="1" Content="{Binding Model.MainContent}"/>
    </Grid>
</Window>
