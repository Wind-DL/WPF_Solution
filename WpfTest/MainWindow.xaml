<Window x:Class="WpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTest"
        xmlns:model="clr-namespace:WpfTest.Model"
        xmlns:converter="clr-namespace:WpfTest.Converter"
        xmlns:zx="clr-namespace:ZX_Controls;assembly=ZX_Controls"
        
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500">
    <Window.Resources>
        <DataTemplate x:Key="datatemplate" DataType="{x:Type model:Student }">
            <Border CornerRadius="3" Background="LightBlue">
                <UniformGrid Columns="2" Rows="2">
                    <TextBlock Text="{Binding Name}"/>
                    <TextBlock Text="{Binding Age}" Grid.Column="1"/>
                    <TextBlock Text="{Binding Address}" Grid.ColumnSpan="2" Grid.Row="1"/>
                </UniformGrid>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ListBox ItemTemplate="{StaticResource datatemplate}"
                 ItemsSource="{Binding Students}"
                 />

    </Grid>
</Window>
