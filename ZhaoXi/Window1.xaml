<Window x:Class="ZhaoXi.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZhaoXi"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid x:Name="grid">
        <Border x:Name="myborder">
            <Border x:Name="border" Height="200" Width="200"
               Background="GreenYellow" BorderThickness="3" BorderBrush="Red"
                CornerRadius="5" >
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Color="LightGray" ShadowDepth="12"/>
            </Border.Effect>
            <TextBlock Width="120" Height="50" TextAlignment="Center"
                       Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorLevel=2,AncestorType=Border},
               Path=Name
                }"
                       />
        </Border>
        </Border>
    </Grid>
</Window>
